{
  "devDependencies": {
    "pagefind": "^1.5.2",
    "typescript": "^6.0.3",
    "vitepress": "2.0.0-alpha.17",
    "vitepress-plugin-pagefind": "^0.4.19",
    "vue": "^3.5.32",
    "wrangler": "^3.0.0"
  },
  "scripts": {
    "dev": "vitepress dev doc",
    "build": "vitepress build doc && node scripts/inject-sitemap-xsl.js",
    "preview": "vitepress preview doc",
    "deploy": "pnpm run build && wrangler deploy"
  }
}
