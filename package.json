{
  "devDependencies": {
    "vitepress": "^1.5.0",
    "wrangler": "^3.0.0"
  },
  "scripts": {
    "dev": "vitepress dev doc",
    "build": "vitepress build doc",
    "preview": "vitepress preview doc",
    "deploy:worker": "pnpm run build && wrangler deploy",
    "deploy:pages": "pnpm run build && wrangler pages deploy doc/.vitepress/dist"
  }
}
